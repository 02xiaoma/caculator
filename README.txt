2016年10月25日
加入了变量的支持
例如：
define pi = 3.14	// 定义一个变量，名字是pi，值是3.14
pi					// 求pi的值
pi*2				// 求2*pi的值
define pi_2 = pi*2	// 定义一个变量，名字是pi_2，值是pi*2
pi_2				// 求pi_2的值

2016年10月24日
用flex和bison实现的一个计算器
功能：
	加减乘除、绝对值、括号、负号
用法：
	手工输入或用某一文件作为输入(如 ./caculator test.txt)